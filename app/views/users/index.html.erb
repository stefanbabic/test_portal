<% provide(:title, "All users") %>
<h1>List of all users:</h1>

<%= will_paginate %>

<ul>
  <% @users.each do |u| %>
    <li><%= link_to "#{u.first_name} #{u.last_name}", u %></li>
  <% end %>
</ul>

<%= will_paginate %>
